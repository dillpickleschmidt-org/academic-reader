# Academic Reader Configuration
# Copy to .env.local: cp .env.example .env.local

# Backend: local | runpod | datalab
BACKEND_MODE=local

# ─────────────────────────────────────────────────────────────
# Datalab (fully managed) - https://www.datalab.to/
# Just need an API key, LLM features included
# ─────────────────────────────────────────────────────────────
DATALAB_API_KEY=

# ─────────────────────────────────────────────────────────────
# Runpod (self-hosted) - https://www.runpod.io/
# Requires deploying the worker Docker image to a Runpod endpoint
# Also requires S3-compatible storage (see Storage section below)
# GOOGLE_API_KEY is optional but improves table/equation extraction
# ─────────────────────────────────────────────────────────────
RUNPOD_API_KEY=
RUNPOD_ENDPOINT_ID=
GOOGLE_API_KEY=

# ─────────────────────────────────────────────────────────────
# S3 Storage (required for Runpod backend)
# Cloudflare R2 for production, MinIO for self-hosted dev (auto-configured)
# ─────────────────────────────────────────────────────────────

# Cloudflare R2 (for Cloudflare Workers deployment):
S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=academic-reader

# ─────────────────────────────────────────────────────────────
# Convex (required for production deployment only)
# Development uses self-hosted Convex via Docker (no config needed)
# For production: run `bunx convex dev` in frontend/ to create deployment
# ─────────────────────────────────────────────────────────────
# CONVEX_DEPLOYMENT=
# CONVEX_URL=
# Google OAuth (required for production)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# ─────────────────────────────────────────────────────────────
# Deployment
# ─────────────────────────────────────────────────────────────
# DEPLOY_API_URL=https://your-api.workers.dev
# DEPLOY_SITE_URL=https://your-site.pages.dev

# ─────────────────────────────────────────────────────────────
# Advanced (usually not needed)
# ─────────────────────────────────────────────────────────────
# CORS_ORIGINS=http://localhost:5173
# API_URL=http://localhost:8787
# LOCAL_WORKER_URL=http://localhost:8000
