name: Build and Deploy TTS Worker

on:
  push:
    branches: [main]
    paths:
      - "tts/**"
      - ".github/workflows/deploy-worker-tts.yml"

jobs:
  build-worker-tts:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v4

      - name: Setup Blacksmith Builder
        uses: useblacksmith/setup-docker-builder@v1

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: useblacksmith/build-push-action@v2
        with:
          context: ./tts
          push: true
          tags: |
            dillpickleschmidt/academic-reader-worker-tts:latest
            dillpickleschmidt/academic-reader-worker-tts:${{ github.sha }}
          provenance: false
