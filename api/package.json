{
  "name": "@academic-reader/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:selfhosted": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "deploy:runpod": "wrangler deploy --env runpod",
    "deploy:datalab": "wrangler deploy --env datalab",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/google": "^3.0.2",
    "@types/katex": "^0.16.7",
    "ai": "^6.0.7",
    "aws4fetch": "^1.0.20",
    "cheerio": "^1.1.2",
    "fonteditor-core": "^2.4.2",
    "hono": "^4.6.0",
    "html-minifier-terser": "^7.2.0",
    "katex": "^0.16.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/html-minifier-terser": "^7.0.2",
    "bun-types": "^1.3.5"
  }
}
